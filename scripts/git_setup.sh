#!/bin/bash
git config --global user.name "bac1957"
git config --global user.email "vassilkov@mail.ru"
git config --global core.editor "code --wait"
git config --global credential.helper store

# Для создания
# git push -u origin main

git clone https://github.com/bac1957/memory.git

